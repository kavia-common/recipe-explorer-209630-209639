import React from "react";

// PUBLIC_INTERFACE
export function SkeletonCard() {
  /** Shimmer placeholder used while loading (simulated). */
  return <div className="skeleton" aria-hidden="true" />;
}
